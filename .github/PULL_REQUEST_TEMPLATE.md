## Evidence (optional)
What was run, what was measured, the numbers - so the reviewer can check the claim, not re-derive it.
- Commands run:
- Tests / checks and their result:
- Measurements (before -> after, with units):
- Not verified (say so explicitly):
